apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: audio-sketchbook-data
  namespace: audio-sketchbook
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
